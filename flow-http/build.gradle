plugins {
    id 'com.android.library'
    id 'kotlin-android'
}

android {
    compileSdkVersion(Version.compileSdkVersion)
    buildToolsVersion(Version.buildToolsVersion)

    defaultConfig {
        minSdkVersion(Version.minSdkVersion)
        targetSdkVersion(Version.targetSdkVersion)
        versionCode(Version.versionCode)
        versionName(Version.versionName)

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }
}

dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])
    implementation(Kotlin.kotlinStdlib)
    implementation(Android.coreKtx)
    implementation(Android.appcompat)
    implementation(Android.material)
    implementation(Kotlin.kotlinReflect)
    implementation(Kotlin.coroutinesAndroid)
    implementation(Kotlin.coroutinesCore)
    implementation(Kotlin.coroutinesJdk)
    implementation(Android.lifecycle.liveDataKtx)
    implementation(Android.lifecycle.viewModelKtx)
    implementation(Android.lifecycle.lifecycleRuntime)
    implementation(Android.lifecycle.viewModelSavestate)
    implementation(Android.lifecycle.process)
    implementation(ThirdParty.retrofit.retrofit)
    implementation(ThirdParty.okhttp.okhttp)
    implementation(ThirdParty.okhttp.okhttpLoggingInterceptor)
    implementation(ThirdParty.retrofit.gson)
}